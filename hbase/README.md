* How to install hbase?
  * run install.sh
  * update /etc/profile
  ```
  export HBASE_HOME=/opt/hbase
  export PATH=$PATH:$HBASE_HOME/bin
  ```
* How to start hbase?
  ```
    ./bin/start-hbase.sh
  ```
